# Domain Docs

**Layout**: Single-context

## Files

| File | Purpose |
|------|---------|
| `CLAUDE.md` | Project architecture, key source files, naming conventions |
| `docs/adr/` | Architectural Decision Records (create as needed) |
| `doc/技术方案.md` | Original WOC technical design doc (historical reference) |

## Rules

When skills like `improve-codebase-architecture`, `diagnose`, or `tdd` need domain context:

1. Read `CLAUDE.md` first for architecture overview
2. Check `docs/adr/` for relevant past decisions
3. The original `doc/技术方案.md` is a historical reference only — current architecture may diverge
